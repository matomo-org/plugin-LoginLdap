{% extends 'admin.twig' %}

{% block content %}
    <div
      vue-entry="LoginLdap.AdminPage"
      servers="{{ servers|default([])|json_encode }}"
      updated-from-pre30="{{ (updatedFromPre30 == 1)|default(false)|json_encode }}"
      ldap-config="{{ ldapConfig|default({})|json_encode }}"
      require-password-confirmation="{{ requirePasswordConfirmation|default(false)|json_encode }}"
    ></div>
{% endblock %}
